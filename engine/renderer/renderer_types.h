#pragma once
#include "defines.h"

typedef struct renderer_state renderer_state;
typedef struct swapchain swapchain;

// TODO: Remove when bindless code takes over
typedef struct draw_context draw_context;